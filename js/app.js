document.addEventListener('DOMContentLoaded', function (){ //Cuando se carga el documento
    
    //Seleccionar los elementos de la interfaz

    const inputEmail = document.querySelector('#email');

    console.log(inputEmail);
});